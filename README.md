# repolayer

> Stop dumping 400k tokens. Index your repos once, let agents query like a database.

A cross-repo index layer for AI coding agents. Pre-compute a structured graph of multiple repositories (modules, exported symbols, call relations, IDL contracts), then query it via MCP from Claude Code, Cursor, or any MCP-compatible agent.

**Status:** Pre-alpha. Not ready for use yet.
